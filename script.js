
// declared a variable, and also use prompt to take input from user
var number = prompt('Enter a number');

// this statement will process the input and give the matching output
if (number > 0){
    document.write('The number is +ve');
} else {
    document.write('It should be a number greater than 0');
}